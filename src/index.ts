/**
 * Re-export types and helper function for use in config files.
 */
export { defineWorktreeConfig } from "./types.js";
export type { WorktreeConfig, TemplateConfig, LoadedConfig } from "./types.js";
